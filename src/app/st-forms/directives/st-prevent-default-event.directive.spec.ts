import { StPreventDefaultEventDirective } from './st-prevent-default-event.directive';

describe('StPreventDefaultEventDirective', () => {
  it('should create an instance', () => {
    const directive = new StPreventDefaultEventDirective();
    expect(directive).toBeTruthy();
  });
});
